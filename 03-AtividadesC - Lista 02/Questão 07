#include <stdio.h>

int main() {
    int numero, soma, resultado;
    
    printf("Digite um numero: ");
    scanf("%d", &numero);
    
    if (numero % 2 == 0) {
        soma = numero + 5;
    }else{
        soma = numero + 8;
    }
    
    printf("Resultado: %d.\n", soma);
    
    return 0;
}
